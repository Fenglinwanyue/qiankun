{
  "name": "qiankun-config",
  "version": "1.0.0",
  "description": "深入微前端架构、寻求业务优化。",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm-run-all --parallel start:*",
    "start:main": "cd main-entry && npm run serve",
    "start:vue": "cd vue-entry && npm run serve",
    "start:react": "cd react-entry && npm run start"
  },
  "keywords": [
    "微前端",
    "qiankun"
  ],
  "author": "cyl",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
